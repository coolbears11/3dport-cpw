The hero background clip lives here as `hero-construction.mp4` (referenced by `components/HeroVideo.jsx`).

If it's ever missing, `HeroVideo` falls back to `/images/hero-poster.svg` automatically — the section still renders correctly with no video file present.
